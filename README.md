# nitinog10/Beta-20 — Documentation

> Auto-generated by [DocuVerse](https://docuverse.ai)

## Overview

BharatTrip AI is an AI-powered travel planning web application aimed at providing users with personalized travel recommendations and insights. The project leverages a React frontend and a Node.js backend to deliver a seamless user experience. The application is structured to offer a variety of features, including personalized trip planning, smart travel insights, and integration with external APIs for real-time data.

The tech stack includes React for the frontend, Node.js for the backend, and various AI models for generating travel recommendations. The project is organized into several key components: the frontend application, backend server, and configuration files for environment setup and AI integration. The `backend` folder contains the server logic, environment configurations, and API testing scripts, while the root directory houses batch and shell scripts for environment setup, backend checks, and application restarts.

Users of this project include travel enthusiasts looking for personalized travel recommendations, developers interested in AI-driven applications, and stakeholders in the travel industry seeking to leverage AI for enhanced user experiences. The documentation provided within the repository guides users through setup, feature exploration, troubleshooting, and integration with personalized trip planners.

## Architecture

```markdown
### Codebase Architecture Overview

#### Organization and Structure

The BharatTrip AI project is organized into several key components and directories, each serving a specific purpose:

- **Root Directory**: Contains essential documentation, setup scripts, and batch files for managing the application.
  - `README.md`: Provides an overview of the project.
  - `QUICK_START.md`: Step-by-step guide to set up and run the application.
  - `setup-env.bat` and `setup-env.sh`: Scripts to set up the environment.
  - `start-react.bat` and `start-react.sh`: Scripts to start the React application.
  - Batch files (`APPLY_FIXES.bat`, `CHECK_BACKEND.bat`, `FIX_NOW.bat`, `RESTART_BACKEND.bat`): Automate various maintenance tasks.

- **Backend Directory (`backend/`)**: Houses the server-side code.
  - `server.js`: Main entry point for the backend server.
  - `generate-alternatives.js`: Contains functions for generating alternative trip plans.
  - `test-apis.js`: Script to test various APIs.
  - `package.json`: Defines dependencies and scripts for the backend.

- **Image Directories (`Beach images data/`, `mountain images data/`, `religion images data/`)**: Store images used in the application.

- **HTML Files**: 
  - `bharattripai.html`, `index.html`, `trip-planner.html`: Front-end HTML files.

#### Data Flow

1. **User Interaction**: Users interact with the front-end via HTML pages (`bharattripai.html`, `trip-planner.html`).
2. **API Requests**: Front-end sends API requests to the backend.
3. **Backend Processing**: 
   - `server.js` handles incoming requests.
   - `generate-alternatives.js` processes requests to generate alternative trip plans.
4. **Response**: Backend sends responses back to the front-end, which updates the UI accordingly.

#### Key Design Patterns

- **Modular Design**: The code is divided into distinct modules (frontend, backend) to separate concerns and improve maintainability.
- **Environment Configuration**: Uses environment variables (`env.example`) to manage configuration settings.
- **Batch Script Automation**: Utilizes batch files to automate common tasks like setup, fixes, and backend management.

#### Main Entry Points

- **Frontend**: `index.html` and `trip-planner.html` are the main HTML entry points.
- **Backend**: `server.js` is the main entry point for the backend server.

This structure allows for clear separation of frontend and backend logic, facilitating easier development, testing, and maintenance.
```

## Folder Structure

```
├── AI_TRIP_ADVISOR_CONFIG.md
├── APPLY_FIXES.bat
├── CHECK_BACKEND.bat
├── CLEANUP_COMPLETE.md
├── FEATURES_SHOWCASE.md
├── FINAL_SUMMARY.md
├── FIX_NOW.bat
├── NEW_FEATURES.md
├── PERSONALIZED_TRIP_PLANNER_INTEGRATION.md
├── QUICK_START.md
├── REACT_VERSION_GUIDE.md
├── README.md
├── RESTART_BACKEND.bat
├── SMART_INSIGHTS_GUIDE.md
├── TROUBLESHOOTING.md
├── bharattripai.html
├── image.png
├── index.html
├── setup-env.bat
├── setup-env.sh
├── start-react.bat
├── start-react.sh
├── temp_planner.txt
├── test.html
├── trip-planner.html
├── ziro.png
├── Beach images data/
│   ├── Agonda Beach.jpg
│   ├── Auroville Beach.jpg
│   ├── Baga Beach.jpg
│   ├── Cherai Beach.jpg
│   ├── Colva Beach.jpg
│   ├── Digha Beach.jpg
│   ├── Ganpatipule Beach.png
│   ├── Gokarna Beach.jpg
│   ├── Kovalam Beach.jpg
│   ├── Mandrem Beach.jpg
│   ├── Marina Beach.jpg
│   ├── Om Beach.jpg
│   ├── Palolem Beach.jpg
│   ├── Paradise Beach.jpg
│   ├── Puri Beach.jpg
│   ├── Radhanagar-Beach.png
│   ├── Rushikonda Beach.jpg
│   ├── Tarkarli Beach.png
│   ├── Varkala Beach.jpg
│   ├── Velsao Beach.jpg
│   └── image.png
├── backend/
│   ├── README.md
│   ├── env.example
│   ├── generate-alternatives.js
│   ├── package.json
│   ├── server.js
│   └── test-apis.js
├── mountain images data/
│   ├── Anamudi (1).jpg
│   ├── Aravalli Range.jpg
│   ├── Chembra Peak.png
│   ├── Dzukou Valley.jpg
│   ├── Eastern Ghats.jpg
│   ├── Himalayan Ladakh Range.jpg
│   ├── Kailash Range.jpg
│   ├── Kamet.jpg
│   ├── Kedarnath.jpg
│   ├── Kudremukh.jpg
│   ├── Nanda Devi.jpg
│   ├── Nilgiri Hills.jpg
│   ├── Nun Kun (Nun Peak).jpg
│   ├── Pir Panjal Range.jpg
│   ├── Sandakphu.jpg
│   ├── Stok Kangri.jpg
│   ├── Trishul.jpg
│   ├── Tungnath.jpg
│   ├── Western Ghats.jpg
│   └── kangchenjunga.jpeg
└── religion images data/
    ├── Prem-Mandir-Vrindavan.jpg
    ├── Rameshwaram.jpg
    ├── Tajulmasajid bhopal.jpg
    ├── delhi-gurudwara-bangla-sahib.jpg
    ├── golden temple amritsar.jpeg
    ├── jagannath puri.jpg
    ├── kamakhya devi temple.jpg
    ├── kedarnath.jpg
    ├── sharif dargah ajmer.jpg
    ├── shirdi.jpg
    └── st john church McLeodGanj.jpg
```

## Dependencies

No dependency manifest found.

## File Documentation

### AI_TRIP_ADVISOR_CONFIG.md

#### Overview

# AI_TRIP_ADVISOR_CONFIG.md

#### Module Overview

This file provides the configuration details for the BharatTrip Maps AI, an intelligent Indian tourism map assistant integrated into the BharatTrip AI travel planning application. It outlines the setup, appearance, and functionality of the ElevenLabs voice assistant widget.

#### Dependencies

- **ElevenLabs Conversational AI**: The core service providing the voice assistant capabilities.

#### Configuration

### Agent Details
- **Name**: BharatTrip Maps AI
- **Agent ID**: `agent_1101k8xp04j6ebesx1jnqhm7djwb`
- **Type**: Conversational AI with voice capabilities
- **Language Support**: English, Hindi, and other Indian languages

### System Prompt / Agent Instructions
The detailed instructions for the agent are provided in the markdown content above. This includes the role, interaction style, constraints, and example responses.

### Configuration Steps
1. **Access ElevenLabs Dashboard**
   - Log in and navigate to the specified agent.
2. **Update Agent Settings**
   - Configure basic information, voice settings, and conversation settings as detailed in the document.
3. **Knowledge Base Integration (Optional)**
   - Integrate Indian Destinations Database and App-Specific Knowledge if supported.
4. **Testing Prompts**
   - Use the provided queries to test the agent's functionality.

### Widget Appearance
- **Position**: Bottom-right corner, floating above content.
- **Visual Style**: Red-orange gradient, compact size, smooth fade-in animation.

### User Experience Flow
- **Scenarios**: New user exploring map, planning a trip, seeking food recommendations.

### Integration with App Features
- **Sync with Map**: Reference current location, weather layer, map style, search history.
- **Sync with Trip Planner**: Suggest destinations, explain budget options, recommend travel modes.
- **Sync with Chat**: Log voice queries, provide context, ensure multilingual continuity.

### Sample Conversation Scripts
- **Scripts**: Complete map guidance, destination recommendation, weather assistance.

### Success Metrics
- **Usage Metrics**: Voice interactions, conversation length, common queries, user satisfaction.
- **Feature Adoption**: Voice assistant usage, weather map feature usage, trip planning completion rate.
- **Quality Metrics**: Response accuracy, response time, user retention, language switch frequency.

### Continuous Improvement
- **Regular Updates**: Monthly, seasonal, quarterly, yearly.
- **User Feedback Integration**: Collect ratings, analyze drop-off points, identify confusing responses, add requested features.

### Troubleshooting
- **Widget Not Appearing**: Check script, verify agent ID, check browser console, ensure internet connection.
- **Voice Not Working**: Check microphone permissions, test browser compatibility, verify service status, refresh page.
- **AI Not Understanding Queries**: Speak clearly, use simple sentences, provide context, try text chat.

#### Notes

- Ensure the agent ID is correctly configured in the ElevenLabs dashboard.
- Regularly update the knowledge base with new destinations and seasonal events.
- Monitor user feedback closely to identify areas for improvement.
- Test the widget across different browsers and devices for compatibility.

### APPLY_FIXES.bat

#### Overview

# APPLY_FIXES.bat

This batch script automates the process of applying fixes to the BharatTrip AI application, ensuring that the backend and frontend are updated and running correctly.

#### Dependencies

- **taskkill**: Used to forcefully terminate processes.
- **timeout**: Provides a delay between steps.
- **cd**: Changes the current directory.
- **start**: Launches a new command prompt or application.

#### Functions

| Command | Parameters | Returns | Description |
|---------|------------|---------|-------------|
| `taskkill /F /IM node.exe` | None | None | Forcefully terminates all `node.exe` processes. |
| `start /MIN cmd /k "echo Starting BharatTrip AI Backend... && npm start"` | None | None | Opens a new command prompt in minimized mode to start the backend. |
| `start "" "bharattrip-react.html"` | None | None | Launches the `bharattrip-react.html` application in a new window. |

#### Notes

- Ensure that `node.exe` processes are not critical to other tasks before running this script.
- The script assumes that `npm start` is correctly configured in the `backend` directory.
- The `bharattrip-react.html` file should be located in the same directory as this batch script.
- The script provides a series of steps to verify that the fixes have been applied successfully.

### CHECK_BACKEND.bat

#### Overview

# CHECK_BACKEND.bat

This batch file runs a series of checks to ensure the BharatTrip AI Backend is operational and responsive. It tests the health endpoint and a specific endpoint for cultural insights, providing feedback on the backend's status.

#### Dependencies

This file does not rely on external libraries or modules.

#### Classes

| Class | Purpose | Key Methods |
| --- | --- | --- |
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `curl` | URL, Headers, Data | HTTP response | Sends HTTP requests to backend endpoints |

#### Configuration

No configuration settings are required for this script.

#### Notes

- The script checks the health endpoint at `http://localhost:3001/api/health`.
- It posts a test request to `http://localhost:3001/api/cultural/insights`.
- If an error is encountered, it suggests restarting the backend using `RESTART_BACKEND.bat`.
- The script pauses at the end, waiting for user input before closing.

### CLEANUP_COMPLETE.md

#### Overview

# Module Overview
This file documents the final cleanup and organization of the BharatTrip AI project, detailing removed files, remaining essential files, and the project's readiness for production and hackathon presentation.

#### Dependencies

None. This is a markdown file for documentation purposes only.

#### Configuration

None. Configuration details are covered in separate files as noted in the documentation.

#### Notes

- The project is now streamlined for production with a single-file frontend and a clean backend.
- All essential documentation is included, ensuring comprehensive guidance for users and configurators.
- The project is ready for deployment on static hosting services for the frontend and Node.js hosting services for the backend.
- Ensure to update the `API_BASE_URL` in `bharattrip-react.html` post-deployment to point to your deployed backend server.
- The project is designed to be India-specific, leveraging local APIs and cultural insights for a tailored travel experience.

### FEATURES_SHOWCASE.md

#### Overview

# Module Overview

The `FEATURES_SHOWCASE.md` file provides a comprehensive overview of the BharatTrip AI platform's features, showcasing its unique selling points, technical innovations, and user benefits. This file serves as a high-level documentation for stakeholders, developers, and potential users to understand the platform's capabilities and competitive advantages.

#### Dependencies

- **OpenWeatherMap API**: Provides real-time weather data and forecasts.
- **Google Maps API**: Offers location-based services, including geocoding and route optimization.
- **Google Places API**: Enhances location search with detailed place information.
- **OpenAI GPT-3.5**: Powers the text-based AI chat for intelligent trip planning.
- **ElevenLabs**: Provides the voice AI trip advisor for hands-free guidance.
- **Leaflet**: Used for rendering the interactive weather map.
- **React**: The UI framework for building the real-time display components.

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| N/A | N/A | N/A |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| N/A | N/A | N/A | N/A |

#### Configuration

- **API Keys**: Required for accessing OpenWeatherMap, Google Maps, and Google Places APIs.
- **AI Model Parameters**: Configurations for the GPT-3.5 and ElevenLabs models.

#### Constants

- **VISIT_SCORE_RANGE**: Defines the range for the visit score (0-100).
- **OPTIMAL_TEMPERATURE_RANGE**: Specifies the optimal temperature range for travel (15-28°C).
- **OPTIMAL_HUMIDITY_RANGE**: Defines the optimal humidity range for travel (40-70%).

#### Notes

- **Data Freshness**: Ensure that the weather and crowd data are updated in real-time for accurate insights.
- **API Rate Limits**: Be mindful of the rate limits for the external APIs to avoid service disruptions.
- **User Privacy**: Handle user data with care, especially location and search history, to maintain privacy standards.
- **Localization**: The platform is tailored for Indian destinations; ensure any updates consider regional specifics.

### FINAL_SUMMARY.md

#### Overview

# Module Overview
This file provides a comprehensive summary of the BharatTrip AI project after cleanup, detailing the changes made, current project structure, removed files, and key features. It serves as a quick reference for team members and new developers to understand the project's current state and how to run it.

#### Dependencies

- **None**: This markdown file does not import any modules or dependencies. It's a static documentation file.

#### Configuration

- **env.config**: Contains API keys and configurations for the backend server. Ensure this file is properly set up and included in `.gitignore` to prevent exposure of sensitive information.

#### Notes

- **Security**: API keys are stored in the backend and not exposed to the frontend. Ensure `.env` files are added to `.gitignore`.
- **Deployment**: The project is ready for deployment. The frontend can be deployed to Netlify, Vercel, or GitHub Pages, and the backend can be deployed to Railway, Render, or Heroku.
- **Documentation Order**: New team members should follow the recommended order of reading documentation to get up to speed quickly.
- **Success Metrics**: The cleanup resulted in a 48% reduction in file count, 67% reduction in HTML versions, and 50% reduction in start scripts, significantly improving maintainability and code duplication.

### FIX_NOW.bat

#### Overview

# FIX_NOW.bat

This batch file automates the process of resetting and restarting the BharatTrip AI backend and launching the frontend application. It ensures that the backend is stopped, restarted, and the frontend is opened in a browser for testing.

#### Dependencies

None.

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `taskkill` | `/F /IM node.exe /FI "WINDOWTITLE eq npm*"` | N/A | Kills the old backend process. |
| `timeout` | `/t 2 /nobreak` | N/A | Pauses for 2 seconds. |
| `start /MIN cmd /c "npm start"` | N/A | N/A | Starts the backend using npm in a minimized command window. |
| `timeout` | `/t 8 /nobreak` | N/A | Pauses for 8 seconds to allow backend initialization. |
| `start "" "bharattrip-react.html"` | N/A | N/A | Opens the frontend application in a browser. |

#### Notes

- The script waits for the backend to initialize before opening the browser.
- Ensure that the backend directory is in the correct path relative to where the script is executed.
- The script pauses at the end to allow the user to observe the backend running on `http://localhost:3001`.

### NEW_FEATURES.md

#### Overview

# NEW_FEATURES.md

#### Module Overview

This file documents the new features added to BharatTrip AI: Cultural Insights and Smart Multi-City Planner. It outlines what each feature does, how to use them, and their integration points with other parts of the application.

#### Dependencies

- **OpenAI GPT-3.5**: For generating cultural insights.
- **React**: Frontend framework for building UI components.
- **Axios**: For making HTTP requests to backend APIs.
- **Styled-components**: For theming and styling UI elements.

#### Classes

None.

#### Functions

None.

#### Configuration

None.

#### Constants

None.

#### Notes

- **Cultural Insights**: The AI-generated content is continuously improving. Monitor accuracy and update system prompts as needed.
- **Smart Planner**: Ensure the AI optimization algorithms account for real-time changes in transport costs and availability.
- **UI Design**: Maintain consistency with the existing theme and component design across both new features.
- **Mobile Responsiveness**: Test both features on various devices to ensure a seamless user experience.
- **Pro Tips**: Encourage users to leverage these tips for the best experience with the new features.

### PERSONALIZED_TRIP_PLANNER_INTEGRATION.md

#### Overview

# PERSONALIZED_TRIP_PLANNER_INTEGRATION.md

#### Module Overview

This document outlines the integration of the personalized AI trip planner into the BharatTrip AI application. It details the enhancements, technical changes, and benefits of this integration.

#### Dependencies

| Library/Module | Purpose |
|----------------|---------|
| `axios` | HTTP requests |
| `react` | Frontend framework |
| `express` | Backend framework |
| `openai` | AI model integration |

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| `TripPlannerPage` | Enhanced trip planner component | `handleInputChange`, `generateItinerary`, `showAlternatives` |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `generateItinerary` | `destination`, `interests`, `dates`, `budget`, `travelMode` | Itinerary object | Generates a structured itinerary based on user input |
| `getAlternatives` | `activityId` | Array of alternative activities | Fetches alternative activities for a given activity ID |

#### Configuration

| Key | Description |
|-----|-------------|
| `API_BASE_URL` | Base URL for API requests |
| `OPENAI_API_KEY` | API key for accessing the AI model |

#### Notes

- Ensure the backend is running before testing the frontend.
- The system has fallback mechanisms to handle cases where new endpoints are unavailable.
- The new design is responsive and provides a better user experience on both mobile and desktop.
- The integration is backward compatible with the previous trip planner.

Feel free to reach out if you have any questions or need further clarification on any part of the integration.

### QUICK_START.md

#### Overview

# 🚀 BharatTrip AI - Quick Start Guide

This guide provides a step-by-step process to set up and run the BharatTrip AI project on your local machine.

#### Dependencies

- `npm`: Node Package Manager, used for installing project dependencies.
- `setup-env.bat` / `setup-env.sh`: Shell scripts to configure the environment.
- `start.bat` / `start.sh`: Shell scripts to start the backend server.

#### Setup Environment

Choose your OS to configure the environment:

### Windows:
```bash
# Double-click setup-env.bat
# OR run in terminal:
setup-env.bat
```

### Mac/Linux:
```bash
chmod +x setup-env.sh
./setup-env.sh
```

#### Install Backend

Navigate to the backend directory and install dependencies:

```bash
cd backend
npm install
cd..
```

#### Start Everything

Start the project using the provided scripts:

### Windows:
```bash
# Double-click start.bat
# OR run in terminal:
start.bat
```

### Mac/Linux:
```bash
chmod +x start.sh
./start.sh
```

#### What's Working Now

- ✨ **AI Chatbot** - Ask travel questions in multiple languages
- 🗺️ **Interactive Maps** - With your Google Maps API
- 🌤️ **Live Weather** - Real weather data
- 🤖 **AI Trip Planning** - Personalized itineraries
- 📍 **Location Search** - Find any place in India

#### Test Your Setup

Run tests to verify all APIs are working correctly:

```bash
cd backend
npm test
```

#### Security Note

Your API keys are stored in `backend/.env`. **NEVER** share this file or commit it to Git!

---

#### 🎤 AI Trip Advisor (Voice Assistant)

The app includes an ElevenLabs voice assistant widget that appears as a floating button.

### What It Does

- Voice-enabled travel guidance
- Helps navigate the weather map
- Recommends nearby attractions and food
- Answers travel questions about India

### Configuration

To customize the voice assistant, follow the guide in [AI_TRIP_ADVISOR_CONFIG.md](AI_TRIP_ADVISOR_CONFIG.md).

The agent should be configured as **"BharatTrip Maps AI"** with expertise in:

- Indian tourism and destinations
- Map navigation and weather layers
- Travel routes and recommendations
- Local culture and food

---

#### 🚨 Troubleshooting

### "Backend Offline"?

- Check if port 3001 is free
- Make sure you ran `npm install` in backend folder

### No AI Responses?

- Your OpenAI key might need credits
- Check console for error messages

### Map Not Loading?

- Refresh the page after backend connects
- Check browser console for errors

#### Notes

- Ensure you have the necessary permissions to execute the shell scripts.
- If you encounter issues, refer to the troubleshooting section for common problems and solutions.

Enjoy exploring India with BharatTrip AI! 🇮🇳

### REACT_VERSION_GUIDE.md

#### Overview

# REACT_VERSION_GUIDE.md

#### Module Overview

This file provides a comprehensive guide to the React-based version of BharatTrip AI, detailing new features, architecture, usage, customization, performance metrics, troubleshooting tips, security notes, and deployment instructions.

#### Dependencies

- **React**: Core library for building user interfaces.
- **Tailwind CSS**: Utility-first CSS framework for styling.
- **Leaflet**: JavaScript library for interactive maps.
- **OpenAI API**: For AI-generated content and chatbot functionality.
- **Google Maps API**: For geocoding and map display.
- **OpenWeather API**: For real-time weather data.

#### Classes

None. This guide is a markdown file and does not contain any classes.

#### Functions

None. This guide is a markdown file and does not contain any functions.

#### Configuration

- **API_BASE**: Base URL for backend API calls. Default is `localhost:3001/api`.

#### Constants

None. This guide does not define any constants.

#### Notes

- Ensure backend is running before using frontend features.
- All API keys are stored server-side in the backend `.env` file.
- Frontend makes calls only to the backend, not directly to external APIs.
- CORS is enabled for development; restrict in production.
- For production deployment, update `API_BASE` in `bharattrip-react.html` to your backend URL.

### README.md

#### Overview

# BharatTrip AI - Your Smart Travel Friend 🇮🇳

A modern, AI-powered travel planning web application designed to help users explore and plan trips across India with personalized recommendations and smart features.

#### Module Overview

This `README.md` file provides an overview of the BharatTrip AI project, including quick start guides, feature descriptions, installation instructions, and future enhancements.

#### Dependencies

- **Font Awesome**: For icons.
- **Google Fonts**: Specifically Poppins for consistent typography.
- **Vanilla JavaScript**: For frontend functionality.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `startReact` | None | None | Starts the React application. Platform-specific scripts are provided. |

#### Configuration

- **Environment Setup**: Initial environment setup scripts for Windows and Mac/Linux are provided.
- **Backend Dependencies**: Backend dependencies are managed via `npm`.

#### Notes

- The application is designed to be fully responsive, ensuring a seamless experience across desktop, tablet, and mobile devices.
- Future enhancements aim to integrate real-time data, social features, advanced AI capabilities, and direct booking functionalities.

### RESTART_BACKEND.bat

#### Overview

# RESTART_BACKEND.bat

This batch file automates the process of restarting the BharatTrip AI Backend service. It stops any existing backend processes and starts a fresh server instance.

#### Dependencies

None.

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|------------|
| N/A   | N/A     | N/A        |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `taskkill` | `/F /IM node.exe /FI "WINDOWTITLE eq npm*"` | N/A | Kills any running node processes with a title containing "npm". |
| `timeout` | `/t 2 /nobreak > nul` | N/A | Waits for 2 seconds. |
| `start` | `cmd /k "npm start"` | N/A | Opens a new command prompt and starts the backend server. |

#### Configuration

None.

#### Notes

- Ensure that the backend directory is correctly referenced in the script.
- The script waits for 2 seconds after killing processes to ensure they are fully terminated before starting a new instance.
- The backend service is started on `http://localhost:3001`. Refresh your browser to connect after the restart.
- The `pause` command at the end keeps the command window open until the user presses a key, allowing for manual inspection of any output or errors.

### SMART_INSIGHTS_GUIDE.md

#### Overview

# 🧠 Smart Travel Insights - AI-Powered Analysis Guide

#### Module Overview

This file provides a comprehensive guide on how the Smart Insights feature uses AI to analyze multiple data sources and deliver intelligent recommendations for the best travel times, optimal routes, and crowd avoidance strategies.

#### Dependencies

- **OpenWeatherMap API**: Provides real-time weather data.
- **Google Maps Directions API**: Offers route optimization and transport data.
- **AI-powered algorithms**: Analyzes crowd density and predicts optimal visiting times.

#### Classes

| Class | Purpose | Key Methods |
|-------|---------|-------------|
| `SmartInsights` | Manages the overall analysis process | `generateInsights()`, `calculateVisitScore()` |

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `getWeatherData` | `destination` | `weatherData` | Fetches real-time weather data from OpenWeatherMap API. |
| `getCrowdDensity` | `destination`, `dateRange` | `crowdDensity` | Analyzes crowd patterns using AI algorithms. |
| `getTransportOptions` | `origin`, `destination`, `mode` | `transportOptions` | Retrieves optimal transport routes from Google Maps API. |
| `calculateVisitScore` | `temperature`, `humidity`, `rainDays` | `visitScore` | Computes a comprehensive visit score based on weather and crowd data. |

#### Configuration

- **API Keys**: Stored in `.env` file for OpenWeatherMap and Google Maps APIs.
- **Data Update Frequency**: Weather data updates every hour; crowd data reanalyzed daily.

#### Constants

- **IDEAL_TEMPERATURE_RANGE**: `15-28°C`
- **IDEAL_HUMIDITY_RANGE**: `40-70%`
- **RAIN_PENALTY_PER_DAY**: `5`

#### Notes

- Ensure API keys are correctly set in the `.env` file to avoid data fetch failures.
- The `calculateVisitScore` function heavily penalizes temperatures outside the ideal range and high rain probabilities.
- Crowd density analysis may have an accuracy range of 75-85% depending on the location.
- Real-time transport data includes traffic updates, which may affect route recommendations.

### TROUBLESHOOTING.md

#### Overview

# TROUBLESHOOTING.md

#### Module Overview

This file provides a comprehensive troubleshooting guide for common issues encountered while working with the BharatTrip AI backend. It outlines quick fixes, diagnostic steps, and preventive measures to ensure smooth operation.

#### Dependencies

- **Node.js**: Required for running the backend server.
- **npm**: Used for managing dependencies.
- **OpenAI API**: Required for certain features like Cultural Insights and Smart Planner.

#### Classes

None

#### Functions

None

#### Configuration

- **.env**: Contains environment variables including `OPENAI_API_KEY`.

#### Constants

None

#### Notes

- **Most Common Fix**: Restarting the backend resolves 90% of issues.
- **Backend Terminal**: Keep it visible to catch errors immediately.
- **API Keys**: Ensure `OPENAI_API_KEY` is valid and has sufficient credits.
- **Port 3001**: Ensure it's free and not used by another application.

### bharattripai.html

### index.html

### setup-env.bat

#### Overview

# setup-env.bat

**Module Overview**

This batch file sets up the environment for the BharatTrip AI project by creating a `.env` file from `env.config`, which contains essential API keys for services like OpenAI, Weather, and Google Maps.

**Dependencies**

- None

**Functions**

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `echo` | None | None | Prints messages to the console. |
| `cd` | Directory | None | Changes the current directory. |
| `if exist` | File | None | Checks if a file exists. |
| `set /p` | Variable=Prompt | Variable value | Prompts user input. |
| `copy` | Source Destination | None | Copies a file. |
| `pause` | None | None | Pauses execution to allow user interaction. |

**Configuration**

- `env.config`: Configuration file used to create the `.env` file.
- `.env`: Environment file containing API keys.

**Notes**

- The `.env` file is crucial for the backend to function correctly.
- The script checks if the `.env` file already exists and prompts the user to confirm if they want to overwrite it.
- Security is emphasized: never share or commit the `.env` file, keep API keys secret, and consider regenerating keys after development.
- If the `.env` file creation fails, the user is instructed to manually rename `env.config` to `.env`.

### setup-env.sh

#### Overview

# setup-env.sh

This script sets up the environment for the BharatTrip AI project by creating a `.env` file from `env.config` and prompting the user to overwrite existing configurations.

#### Dependencies

This script does not rely on external libraries or dependencies.

#### Functions

| Function | Parameters | Returns | Description |
|----------|------------|---------|-------------|
| `setup-env` | None | None | Sets up the `.env` file from `env.config` and prompts for overwriting if the `.env` file already exists. |

#### Configuration

No configuration is required for this script.

#### Notes

- The script checks if the `.env` file already exists and prompts the user for confirmation before overwriting it.
- If the `.env` file creation fails, the user is instructed to manually rename `env.config` to `.env`.
- Important security notes are printed to remind users to keep `.env` secure and not to share or commit it.

### start-react.bat

#### Overview

# start-react.bat

**Module Overview**

This batch file initiates the BharatTrip AI application, starting the backend server and opening the React frontend in a browser. It ensures both backend and frontend are running and accessible.

**Dependencies**

None

**Functions**

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `echo` | Text | None | Prints text to the console. |
| `cd` | Directory | None | Changes the current directory. |
| `start` | URL/Path | None | Opens a new command window to run a specified program or command. |
| `timeout` | Duration | None | Waits for a specified number of seconds without processing any commands. |

**Configuration**

None

**Notes**

- The backend server starts in a new command window which must be closed manually to stop the application.
- The `timeout /t 5 /nobreak > nul` command waits 5 seconds for the backend to initialize before opening the frontend.
- The application features an AI chatbot, interactive maps, an AI trip planner, and a modern UI with dark/light mode options.
- To stop the application, close the backend terminal window.

### start-react.sh

#### Overview

# start-react.sh

#### Module Overview

`start-react.sh` is a shell script that initializes the BharatTrip AI project, starting both the backend server and the React frontend, and then opens the application in the default web browser.

#### Dependencies

- **bash**: The script is written in bash shell.
- **npm**: Used to start the backend server.
- **open**: Command to open the browser on macOS.
- **xdg-open**: Command to open the browser on Linux.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| None | None | None | Starts the backend server, waits for it to be ready, opens the React frontend in the browser, and handles graceful shutdown. |

#### Notes

- The script assumes that the `backend` and `frontend` directories are in the same directory as the script.
- It waits for 5 seconds before opening the React frontend to ensure the backend is up and running.
- The script uses `OSTYPE` to determine the operating system and opens the browser accordingly.
- On Linux, if `xdg-open` fails, it will prompt the user to open `bharattrip-react.html` manually.
- Pressing `Ctrl+C` will trigger a graceful shutdown, stopping the backend server and exiting the script.

### temp_planner.txt

#### Overview

# `temp_planner.txt` Documentation

#### Module Overview

This file contains the `EnhancedTripPlannerPage` component, which is a React functional component for a trip planning interface. It allows users to input travel preferences and generate a personalized itinerary using either an AI planner or a smart planner.

#### Dependencies

- `useState` from `react`: Manages component state.
- `fetch`: Makes HTTP requests to the backend API.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `EnhancedTripPlannerPage` | None | JSX | Renders the trip planning interface and handles user interactions. |

#### Configuration

- `API_BASE`: Base URL for the backend API.

#### Notes

- The `generateItinerary` function requires both a destination and at least one interest to be selected. Otherwise, it displays an alert.
- The `toggleInterest` function adds or removes an interest from the form data based on whether it's already selected.
- The `handleViewOnMap` function is currently incomplete and should be implemented to handle map interactions.
- The component uses Tailwind CSS for styling.

### test.html

#### Overview

# test.html

#### Module Overview

`test.html` is a simple HTML file that serves as a basic React application showcasing a navigation bar and two pages: a home page and a trip planner page. It uses Tailwind CSS for styling and Font Awesome for icons.

#### Dependencies

| Library | URL |
| --- | --- |
| React | https://unpkg.com/react@17/umd/react.development.js |
| ReactDOM | https://unpkg.com/react-dom@17/umd/react-dom.development.js |
| Babel | https://unpkg.com/@babel/standalone/babel.min.js |
| Tailwind CSS | https://cdn.tailwindcss.com |
| Font Awesome | https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `App` | None | None | Main React component that manages state and renders the navigation and pages. |

#### Notes

- The `App` component uses React hooks to manage the state of the current page.
- The navigation bar dynamically changes styles based on the current page.
- The `PlannerPage` component currently only displays a placeholder text.

### trip-planner.html

#### Overview

# trip-planner.html Documentation

#### Module Overview

This file is the main entry point for the BharatTrip AI Planner web application. It sets up the HTML structure, includes necessary CSS and JavaScript libraries, and defines the React component for the trip planner interface.

#### Dependencies

- **Tailwind CSS**: Provides utility-first CSS classes for styling.
- **Font Awesome**: Offers icon fonts used throughout the UI.
- **React**: The core library for building the UI components.
- **Babel**: Allows us to use modern JavaScript features.

#### Classes

None. This file uses a functional component approach.

#### Functions

| Function             | Parameters | Returns | Description |
|----------------------|------------|---------|-------------|
| `TripPlannerPage`    | None       | React component | The main React component rendering the trip planner interface. |
| `toggleInterest`     | `interest` | None    | Toggles the selection of an interest. |
| `isFormValid`        | None       | `boolean` | Checks if the form inputs are valid. |
| `generateItinerary`  | None       | None    | Asynchronously generates an itinerary and sets it in the state. |

#### Configuration

None.

#### Constants

None.

#### Notes

- The `generateItinerary` function currently uses hardcoded data for demonstration purposes. In a production environment, this should be replaced with an API call to fetch real itinerary data.
- The form validation in `isFormValid` is basic. Additional checks may be needed based on business requirements.
- Ensure to handle edge cases for date inputs to avoid invalid date ranges.

### backend/README.md

#### Overview

# BharatTrip AI Backend Setup Guide

#### Module Overview

This file provides a comprehensive guide to setting up and running the BharatTrip AI backend. It covers dependency installation, environment configuration, API endpoint details, testing methods, troubleshooting tips, production deployment options, and API rate limits.

#### Dependencies

- `npm`: Package manager for Node.js, used to install backend dependencies.
- `dotenv`: Loads environment variables from a `.env` file into `process.env`.
- `express`: Web framework for Node.js, used to create API endpoints.
- `axios`: Promise-based HTTP client for making requests to external APIs.
- `openai`, `openweathermap`, `google-maps`: Client libraries for interacting with respective APIs.

#### Configuration

- `.env`: Environment variables file. Contains API keys and other configuration settings.
  - `OPENAI_API_KEY`: API key for OpenAI services.
  - `WEATHER_API_KEY`: API key for OpenWeatherMap services.
  - `GOOGLE_MAPS_API_KEY`: API key for Google Maps services.

#### API Endpoints

### Health Check
- **GET** `/api/health`: Returns a simple health check response to verify backend is running.

### AI Chatbot
- **POST** `/api/chatbot`: Accepts a message and language, returns AI-generated response.

### Weather Data
- **GET** `/api/weather/:location`: Returns weather data for a given location.
- **GET** `/api/weather/:location?lat=[lat]&lon=[lon]`: Returns weather data for given coordinates.

### Places Search
- **POST** `/api/places/search`: Searches for places based on query, location, radius, and type.

### Nearby Places
- **POST** `/api/places/nearby`: Finds nearby places based on location, radius, and types.

### Geocoding
- **GET** `/api/geocode/:address`: Converts an address to geographic coordinates.

### Trip Planning
- **POST** `/api/trip/plan`: Plans a trip based on destination, interests, duration, budget, and travel mode.

### Landmark Recognition
- **POST** `/api/landmark/recognize`: Recognizes landmarks from a base64 encoded image.

### Smart Insights & Analysis
#### AI-Powered Travel Recommendations
- **POST** `/api/recommendations/analyze`: Analyzes travel recommendations based on destination, dates, and interests.

#### Real-time Crowd Density
- **POST** `/api/crowd/density`: Provides real-time crowd density information for a place.

#### Public Transport Routes
- **POST** `/api/transport/routes`: Finds optimized public transport routes between origin and destination.

#### Climate Trends Analysis
- **GET** `/api/climate/trends/:destination?months=[months]`: Analyzes climate trends for a destination over a specified number of months.

#### Testing the Backend

### Using cURL
Examples provided for testing health check, chatbot, and geocoding endpoints.

### Using the Frontend
Instructions for testing backend features through the integrated frontend.

#### Troubleshooting

Common issues and solutions for backend startup problems, API errors, and CORS issues.

#### Production Deployment

### Using PM2
Steps to deploy backend using PM2 process manager.

### Using Docker
Dockerfile for containerizing the backend application.

#### API Rate Limits

Important rate limits to be aware of for external APIs used in the backend.

#### Support

Guidelines for troubleshooting issues, including checking logs, verifying API keys, and ensuring network connectivity.

### backend/env.example

#### Overview

# Backend Environment Variables

This file, `backend/env.example`, contains placeholder environment variables for configuring external API services and server settings. It serves as a template for creating a `.env` file with actual API keys and server configuration values.

#### Dependencies

No direct imports are required for this file, as it is a plain text file used for environment variable configuration.

#### Configuration

| Variable | Purpose | Description |
|----------|---------|-------------|
| `PORT` | Server Configuration | Port number on which the server will run. |
| `OPENAI_API_KEY` | OpenAI API Configuration | API key for accessing the OpenAI services. |
| `WEATHER_API_KEY` | OpenWeatherMap API Configuration | API key for accessing weather data from OpenWeatherMap. |
| `GOOGLE_MAPS_API_KEY` | Google Maps API Configuration | API key for accessing Google Maps services. |

#### Notes

- Copy this file to `.env` and fill in the actual API keys for the respective services.
- Ensure the necessary Google Maps APIs are enabled in the Google Cloud Console.
- This file is not part of the codebase; it is used to set environment variables for the application.

### backend/generate-alternatives.js

#### Overview

# backend/generate-alternatives.js

This file exports a single function `generateAlternatives` that uses the `openai` module to generate alternative activities for a given day in an itinerary.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `openai` | Provides access to the `openai` API for generating text based on prompts. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `generateAlternatives` | `currentDay`, `currentActivities`, `destination`, `preferences` | `{ success: boolean, alternatives: Array, error: string }` | Generates alternative activities for a specified day in an itinerary using the `openai` API. |

#### Notes

- The function expects `currentDay` to be a number, `currentActivities` to be a string, `destination` to be a string, and `preferences` to be an object.
- The function returns an object with a `success` flag, an array of `alternatives`, and an optional `error` string.
- If the `openai` API call fails, the function returns a fallback set of alternatives.
- Ensure the `openai` module is properly configured and authenticated before use.

### backend/package.json

#### Overview

# Backend `package.json`

**Module Overview**

This file defines the dependencies and scripts for the `bharattrip-ai-backend` project. It ensures that the backend server can be started, tested, and run with the necessary libraries and tools.

**Dependencies**

| Dependency | Purpose |
|------------|---------|
| `express` | Provides a robust set of features for web and mobile applications. |
| `cors` | Enables cross-origin resource sharing. |
| `dotenv` | Loads environment variables from a `.env` file. |
| `axios` | Promise-based HTTP client for the browser and Node.js. |
| `openai` | Interface for the OpenAPI service. |
| `multer` | Middleware for handling `multipart/form-data`, used for uploading files. |

**Scripts**

| Script | Purpose |
|--------|---------|
| `start` | Starts the backend server using `node server.js`. |
| `dev` | Starts the backend server in development mode using `nodemon server.js`. |
| `test` | Runs the API tests using `node test-apis.js`. |
| `test:api` | Runs the API tests using `node test-apis.js`. |

**Notes**

- The `dev` script uses `nodemon` to automatically restart the server on file changes, which is helpful during development.
- Ensure environment variables are set in the `.env` file for services like `openai` to function correctly.
- The `test` and `test:api` scripts are aliases, both running the same test file `test-apis.js`.

### backend/server.js

#### Overview

# Module Overview
This file sets up the backend server for BharatTrip AI, handling API requests, integrating with third-party services, and serving static files.

#### Dependencies

- `express`: Web framework for handling routes and middleware.
- `cors`: Middleware to enable Cross-Origin Resource Sharing.
- `axios`: HTTP client for making requests to external APIs.
- `dotenv`: Loads environment variables from a `.env` file.
- `path`: Node.js module for handling and transforming file paths.
- `multer`: Middleware for handling `multipart/form-data`, primarily used for uploading files.
- `openai`: SDK for interacting with the OpenAI API.

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `app.post('/api/reviews')` | `req`, `res` | `Promise` | Handles POST requests to save a new review. |
| `app.get('/api/reviews/:placeId')` | `req`, `res` | `Promise` | Handles GET requests to fetch reviews for a specific place. |
| `app.get('/api/tourist-guides/:location')` | `req`, `res` | `Promise` | Handles GET requests to fetch tourist guides for a specific location. |
| `app.get('/api/health')` | `req`, `res` | `Promise` | Health check endpoint to verify server status. |
| `app.post('/api/chatbot')` | `req`, `res` | `Promise` | Handles POST requests to get a response from the chatbot. |
| `app.get('/api/weather/:location')` | `req`, `res` | `Promise` | Handles GET requests to fetch weather data for a location. |
| `app.post('/api/places/search')` | `req`, `res` | `Promise` | Handles POST requests to search for places. |
| `app.post('/api/places/nearby')` | `req`, `res` | `Promise` | Handles POST requests to find nearby places. |
| `app.get('/api/geocode/:address')` | `req`, `res` | `Promise` | Handles GET requests to geocode an address. |

#### Configuration

- `PORT`: Server port, defaults to 3001 if not set in environment variables.
- `OPENAI_API_KEY`: API key for OpenAI, loaded from environment variables.
- `WEATHER_API_KEY`: API key for OpenWeatherMap, loaded from environment variables.
- `GOOGLE_MAPS_API_KEY`: API key for Google Maps, loaded from environment variables.

#### Notes

- Reviews are currently stored in `localStorage`, which is not persistent. In production, this should be replaced with a database.
- The `touristGuides` object is hardcoded. This should be replaced with a dynamic data source in production.
- The geocoding endpoint first tries Google Maps. If it fails, it falls back to OpenStreetMap Nominatim.

### backend/test-apis.js

#### Overview

# backend/test-apis.js

This file contains a script to test the functionality of various APIs within the BharatTrip AI service. It ensures that all endpoints are operational and provides feedback on the success or failure of each test.

#### Dependencies

| Import | Purpose |
| --- | --- |
| `axios` | HTTP client for making requests to the API endpoints. |
| `dotenv` | Loads environment variables from a `.env` file into `process.env`. |

#### Functions

| Function | Parameters | Returns | Description |
| --- | --- | --- | --- |
| `testAPIs` | None | None | Executes a series of tests on the API endpoints and logs the results. |

#### Notes

- Ensure the `.env` file contains all necessary API keys for the tests to run successfully.
- The script checks if the backend server is running before executing the tests.
- If any test fails, the script provides guidance on what to check, such as the validity of API keys and the status of required services.

